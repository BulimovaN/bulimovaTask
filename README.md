# Bulimova Task

REST API для управления пользователями и задачами.

Проект выполнен на **Spring Boot** с использованием **JWT-аутентификации** и **PostgreSQL**.  
Поддерживается запуск как локально, так и через **Docker Compose**.

---

## 🛠 Технологии

- Java 21
- Spring Boot
- Spring Security (JWT)
- Spring Data JPA
- PostgreSQL
- Docker Compose

---

## 🐘 PostgreSQL и порты

Проект использует PostgreSQL, запущенный в Docker..

### Конфигурация портов

- **PostgreSQL**
    - порт внутри контейнера: `5432`
    - порт на хосте: **`5433`**

Это сделано намеренно, чтобы избежать конфликтов с локально установленным PostgreSQL на macOS.

```text
Host (macOS):        localhost:5433
Docker container:    db:5432
